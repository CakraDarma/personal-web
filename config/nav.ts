import { NavConfig } from "types"

export const navConfig: NavConfig = {
  mainNav: [
    {
      title: "Features",
      href: "/#features",
    },
    {
      title: "Blog",
      href: "/blog",
    },
  ],
}
